# binary search still has bug
    # k = start_idx
    # while k != end_idx:
    #     sigma, epsilon = _p(distm, cod_score_, args.idx, x_, sigma=cod_score_sort[k])
    #     stair.append((sigma, epsilon))
    #     k_ = k
    #     sig_, eps_ = get_left_epsilon(stair, sigma = sigma)
    #     if epsilon == eps_:
    #         k = (k + end_idx)//2
    #     else:
    #         k = (k + start_idx)//2
    #     print(f'previous idx is {k_}. Current idx is {k}')
    #     if k == k_: k = k+1

